import styles from "./VoteStats.module.css";
import type { Votes } from "../../types/votes";

export default function VoteStats() {
  return (
    <div>
      <p>Vote Stats component</p>
    </div>
  );
}
